// Escuchador de evento para cargar la imagen
document.addEventListener('DOMContentLoaded', function() {
    const image = document.querySelector('.opacity-transition');
    image.classList.add('loaded'); // Agrega la clase "loaded" para cambiar la opacidad
  });

  document.addEventListener('DOMContentLoaded', function() {
    const image = document.querySelector('.imperial-transition-right');
    image.classList.add('loaded'); // Agrega la clase "loaded" para cambiar la opacidad
  });

  document.addEventListener('DOMContentLoaded', function() {
    const image = document.querySelector('.imperial-transition-left');
    image.classList.add('loaded'); // Agrega la clase "loaded" para cambiar la opacidad
  });
